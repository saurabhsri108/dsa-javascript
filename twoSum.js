const twoSum = (arr, target) => {
  for (let i = 0; i < arr.length; i++) {
    if (hash.arr[i] != null) {
      return [arr[i], hash.arr[i]]
    }
    hash.arr[i] = i
    return
  }
}

const array = [7, 2, 11]
